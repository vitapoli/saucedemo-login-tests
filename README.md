# Тесты авторизации для saucedemo.com
---
# Задача
Автоматизировать тестирование логина на сайте https://www.saucedemo.com/ с использованием Python.
---

# Описание проекта
Проект по автоматизации тестирования функционала авторизации выполнен на языке Python 3.10 с использование фреймворка Selenium и архитектуры Page Object Model (POM). Для управления браузером применён фреймворк Selenium, настроен Docker для изолированного запуска тестов в контейнере. Для организации тестов применён pytest, для визуализации результатов — интеграция с Allure. Все зависимости перечислены в файле requirements.txt

# Частное описание проекта
Проект реализован с выделением отдельного класса LoginPage, инкапсулирующего всю логику взаимодействия со страницей входа. Архитектура позволяет легко расширять проект новыми страницами и тестами без дублирования кода.

### От автора:
Автотесты разработаны для демонстрации своих навыков работодателю. Все учётные записи и сценарии публично доступны и предназначены специально для обучения тестированию. Тесты покрывают важные сценарии, которые является основой для дальнейшего тестирования. В процессе реализации проекта был использован нейросетевой ассистент (Qwen) как инструмент повышения инженерной эффективности.

---

## ✅ Что тестируется

| Тесты | Описание |
|-------|----------|
| 1. Успешный вход | Стандартный пользователь и правильный пароль (`standard_user`) |
| 2. Неверный пароль | Попытка входа нескольких пользователей с неправильным паролем |
| 3. Заблокированный пользователь | Попытка входа заблокированного пользователя `locked_out_user` |
| 4. Пустые поля | Отправка формы без логина и пароля |
| 5. Performance-пользователь | Вход под `performance_glitch_user` (с задержками) |

---

## Технологии

- **Python 3.10**
- **Selenium 4.15** (с автоматической загрузкой драйвера)
- **pytest** — запуск и параметризация тестов
- **Allure** — для вывода отчётов и удобства работы с итоговыми результатами
- **Docker** — для запуска тестов в контейнере

---

## Структура проекта
```
saucedemo-login-tests/
├── pages/
│   └── login_page.py       # Page Object для страницы входа
├── tests/
│   └── test_login.py       # 5 тестов авторизации
├── conftest.py             # Фикстуры и константы
├── requirements.txt        # Зависимости Python
├── Dockerfile              # Конфигурация Docker
├── .gitignore              # Исключения для Git
└── README.md               # Описание
```
---

## ✅ Требования

Перед запуском убедитесь, что установлено:

| Что нужно | Как проверить | Ссылка для установки |
|-----------|---------------|----------------------|
| **Python 3.10+** | `python --version` | https://www.python.org/downloads/ |
| **Docker Desktop** | `docker --version` | https://www.docker.com/products/docker-desktop/ |
| **Git** (опционально) | `git --version` | https://git-scm.com/download/win |

> 💡 **Windows:** При установке Python **обязательно** поставьте галочку ✅ **"Add Python to PATH"**

---

## 🚀 Запуск локально (без Docker)

### для Windows

```powershell
# Клонируйте репозиторий (или скачайте как ZIP)
git clone https://github.com/vitapoli/saucedemo-login-tests.git
cd saucedemo-login-tests

# Создайте виртуальное окружение (рекомендуется)
python -m venv 

# Активируйте его
venv\Scripts\activate

# Установите зависимости
pip install -r requirements.txt
```

> 💡 Если команда pip не найдена:
```powershell
python -m pip install -r requirements.txt
```

```powershell
# Запустите тесты
pytest tests/ -v
```

### Для macOS / Linux:
```powershell
# Создайте виртуальное окружение
python3 -m venv venv

# Активируйте его
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt

# Запустите тесты
pytest tests/ -v
```

## 🚀 Запуск в Docker

### Шаг 1: Убедитесь, что Docker Desktop запущен
1. Откройте Docker Desktop (иконка кита 🐳 в системном трее)
2. Дождитесь зелёного статуса "Docker Desktop is running"
3. Проверьте в терминале:
```powershell
docker --version
```
### Шаг 2: Соберите Docker-образ
```powershell
# Перейдите в папку проекта
cd "C:\Users\My Comp\Desktop\AQA Python\saucedemo-login-test"

# Соберите образ (ждите 3-5 минут при первом запуске)
docker build -t saucedemo-tests .
```
### Шаг 3: Запустите тесты в контейнере
```powershell
docker run --rm saucedemo-tests
```
